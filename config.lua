Config = {
    Lan = "en",
    MinPrice = 800,
    MaxPrice = 1400,
    RenewedPhone = true,
    MechanicJobName = "mechanic",
	RenewedBanking = true, -- Enable if using Renewed-Banking (For transactions history)
    GiveMechanicJobMoney = true, -- If money should go to mechanic job (Only works with RenewedBanking)
    PayProcent = 0.6, -- 0.6 = 60% (Only works with RenewedBanking)
}

Loc = {}